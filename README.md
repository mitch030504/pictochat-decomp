# chaos-data

Auto-generated progress data for pictochat-decomp. `chaos-db.json` is
regenerated on every push to main by `.github/workflows/update-chaos-data.yml`
(from committed data, no ROM). tangos.dev reads its `stats` block.

Do not hand-edit.
